// COMMENT:If we decide to change the API URL, we dont need to change it across the project
// We could just use default const in .env of env.ts in our case
export const API_URL = 'http://localhost:8000/api';
